name: Build and Test

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  build:
    name: Build program
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Gives `docker build` the buildx driver, which is what makes the GitHub Actions
      # cache backend available to the cache mounts in Dockerfile.build.
      - uses: docker/setup-buildx-action@v3

      - name: Build
        run: make build

      # The build succeeding is not enough on its own: --output writes whatever the
      # artifacts stage contains, so an empty or missing .so would still exit 0.
      #
      # The hash is committed because the build is byte-reproducible: the same bytes
      # come out of an emulated amd64 container on arm64 and a native amd64 runner.
      # Any change to the program, the lockfile, or the base image changes it, and the
      # pull request making that change updates program.sha256 in the same commit.
      - name: Verify the artifact
        run: |
          test -s target/deploy/squads_multisig_program.so
          ls -l target/deploy/squads_multisig_program.so
          sha256sum -c program.sha256

  test:
    name: Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: yarn

      - uses: docker/setup-buildx-action@v3

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      # Builds the program with --features testing, then runs the suite against a
      # solana-test-validator in a container. No Anchor CLI and no Solana toolchain
      # are installed on the runner.
      - name: Test
        run: make test
